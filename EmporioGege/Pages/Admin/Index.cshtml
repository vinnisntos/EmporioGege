@page
@model EmporioGege.Pages.Admin.IndexModel
@{
    ViewData["Title"] = "Painel Administrativo";
    Layout = "_Layout";
}

<div class="container-fluid p-0">
    <!-- Cabeçalho de Boas-Vindas -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-white fw-bold">Visão Geral do Negócio</h1>
            <p class="text-muted small mb-0">Monitore o faturamento, estoque e contas pendentes da adega.</p>
        </div>
        <button class="btn btn-sm btn-outline-warning d-flex align-items-center gap-2" onclick="window.location.reload();">
            <i class="bi bi-arrow-clockwise"></i> Atualizar Dados
        </button>
    </div>

    <!-- Grid de Métricas Principais (UX Cards) -->
    <div class="row g-3 mb-4">
        <!-- Card 1: Faturamento -->
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <span class="text-muted small fw-medium text-uppercase">Faturamento (Hoje)</span>
                    <div class="p-2 bg-success bg-opacity-10 text-success rounded-3">
                        <i class="bi bi-cash-coin fs-4"></i>
                    </div>
                </div>
                <h3 class="fw-bold text-white mb-1">@Model.FaturamentoHoje.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))</h3>
                <span class="text-success small fw-medium"><i class="bi bi-graph-up me-1"></i> Caixa em operação</span>
            </div>
        </div>

        <!-- Card 2: Fiado / Penduras -->
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <span class="text-muted small fw-medium text-uppercase">Total no PendurAi</span>
                    <div class="p-2 bg-warning bg-opacity-10 text-warning rounded-3">
                        <i class="bi bi-person-exclamation fs-4"></i>
                    </div>
                </div>
                <h3 class="fw-bold text-white mb-1">@Model.FiadoPendenteTotal.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))</h3>
                <span class="text-muted small">Carteira ativa de clientes</span>
            </div>
        </div>

        <!-- Card 3: Estoque Crítico -->
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <span class="text-muted small fw-medium text-uppercase">Alertas de Estoque</span>
                    <div class="p-2 bg-danger bg-opacity-10 text-danger rounded-3">
                        <i class="bi bi-box-seam-fill fs-4"></i>
                    </div>
                </div>
                <h3 class="fw-bold text-white mb-1">@Model.AlertasEstoqueCritico</h3>
                <span class="text-danger small fw-medium">
                    <a asp-page="/Admin/Estoque" class="text-danger text-decoration-none">Ver itens abaixo do mínimo →</a>
                </span>
            </div>
        </div>

        <!-- Card 4: Comandas em Aberto -->
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <span class="text-muted small fw-medium text-uppercase">Comandas Ativas</span>
                    <div class="p-2 bg-info bg-opacity-10 text-info rounded-3">
                        <i class="bi bi-receipt fs-4"></i>
                    </div>
                </div>
                <h3 class="fw-bold text-white mb-1">@Model.ComandasAtivasContador</h3>
                <span class="text-info small fw-medium">Consumo local no balcão</span>
            </div>
        </div>
    </div>

    <!-- Grid de Resultado do Mês (Faturamento / CMV / Lucro Bruto / ROI) -->
    <div class="row g-3 mb-4">
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <span class="text-muted small fw-medium text-uppercase">Faturamento (Mês)</span>
                <h3 class="fw-bold text-white mb-1 mt-2">@Model.ResumoMes.Faturamento.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))</h3>
                <span class="text-muted small">@Model.ResumoMes.TotalVendas venda(s) fechada(s)</span>
            </div>
        </div>
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <span class="text-muted small fw-medium text-uppercase">CMV (Mês)</span>
                <h3 class="fw-bold text-white mb-1 mt-2">@Model.ResumoMes.Cmv.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))</h3>
                <span class="text-muted small">Custo da mercadoria vendida</span>
            </div>
        </div>
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <span class="text-muted small fw-medium text-uppercase">Lucro Bruto (Mês)</span>
                <h3 class="fw-bold @(Model.ResumoMes.LucroBruto >= 0 ? "text-success" : "text-danger") mb-1 mt-2">@Model.ResumoMes.LucroBruto.ToString("C", System.Globalization.CultureInfo.GetCultureInfo("pt-BR"))</h3>
                <span class="text-muted small">Faturamento − CMV</span>
            </div>
        </div>
        <div class="col-xl-3 col-sm-6">
            <div class="card card-custom h-100 p-4">
                <span class="text-muted small fw-medium text-uppercase">ROI (Mês)</span>
                <h3 class="fw-bold text-white mb-1 mt-2">@(Model.ResumoMes.RoiPercentual is { } roi ? $"{roi:0.0}%" : "—")</h3>
                <span class="text-muted small">Lucro bruto ÷ CMV</span>
            </div>
        </div>
    </div>

    <!-- Seção Inferior: Atalhos Rápidos e Gráficos Futuros -->
    <div class="row g-3">
        <div class="col-lg-8">
            <div class="card card-custom p-4 h-100">
                <h5 class="text-white fw-semibold mb-3">Ações Administrativas Rápidas</h5>
                <div class="row g-2">
                    <div class="col-sm-4">
                        <a asp-page="/Admin/Estoque" class="btn btn-outline-light w-100 py-3 text-start d-flex align-items-center gap-2" style="border-color: rgba(255,255,255,0.08); background: rgba(255,255,255,0.02)">
                            <i class="bi bi-plus-circle text-warning"></i> Novo Produto / Grade
                        </a>
                    </div>
                    <div class="col-sm-4">
                        <a asp-page="/Admin/Clientes" class="btn btn-outline-light w-100 py-3 text-start d-flex align-items-center gap-2" style="border-color: rgba(255,255,255,0.08); background: rgba(255,255,255,0.02)">
                            <i class="bi bi-person-plus text-warning"></i> Autorizar Crédito / Fiado
                        </a>
                    </div>
                    <div class="col-sm-4">
                        <a asp-page="/Admin/Funcionarios" class="btn btn-outline-light w-100 py-3 text-start d-flex align-items-center gap-2" style="border-color: rgba(255,255,255,0.08); background: rgba(255,255,255,0.02)">
                            <i class="bi bi-shield-plus text-warning"></i> Configurar Operador
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card card-custom p-4 h-100">
                <h6 class="text-white fw-semibold mb-3"><i class="bi bi-exclamation-triangle-fill text-warning me-1"></i> Produtos Próximos ao Vencimento</h6>
                @if (Model.ProdutosProximosValidade.Count == 0)
                {
                    <p class="text-muted small mb-0">Nenhum produto vencendo nos próximos 30 dias.</p>
                }
                else
                {
                    <ul class="list-unstyled mb-0" style="max-height: 220px; overflow-y: auto;">
                        @foreach (var p in Model.ProdutosProximosValidade)
                        {
                            var vencido = p.DiasRestantes < 0;
                            var critico = vencido || p.DiasRestantes <= 7;
                            <li class="d-flex justify-content-between align-items-center py-2 border-bottom border-secondary border-opacity-10">
                                <div>
                                    <div class="text-white small fw-medium">@p.Nome</div>
                                    <div class="text-muted small">Estoque: @p.EstoqueAtual</div>
                                </div>
                                <span class="badge @(critico ? "bg-danger" : "bg-warning text-dark")">
                                    @(vencido ? $"Vencido há {-p.DiasRestantes}d" : $"{p.DiasRestantes}d restantes")
                                </span>
                            </li>
                        }
                    </ul>
                }
            </div>
        </div>
    </div>
</div>